apiVersion: v1
kind: Service
metadata:
  name: tienda-backend-despacho
  namespace: inovatech
spec:
  selector:
    app: tienda-backend-despacho
  ports:
    - port: 8081
      targetPort: 8081
  type: ClusterIP